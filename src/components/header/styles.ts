import styled from 'styled-components'

export const HeaderStyled = styled.header`
  select {
    min-width: 160px;
    display: block;
  }

  button {
    min-width: 160px;
  }
`

export const HeaderControlPanel = styled.div`
  margin: 1rem 0 2rem;
  display: flex;
  justify-content: space-between;
`
